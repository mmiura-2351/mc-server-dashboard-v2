"""Placeholder test verifying the test runner is wired up.

Replaced by real behavior tests as domain code lands (docs/dev/TESTING.md).
"""

import mc_server_dashboard_api


def test_package_is_importable() -> None:
    assert mc_server_dashboard_api.__doc__ is not None
