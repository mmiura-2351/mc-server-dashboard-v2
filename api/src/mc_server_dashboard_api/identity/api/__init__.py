"""HTTP edge for the ``identity`` context: thin routers, no business logic.

Routers translate HTTP to a use-case call and back; the use case and its Ports
are supplied by the wiring layer (ARCHITECTURE.md Section 2.1).
"""
