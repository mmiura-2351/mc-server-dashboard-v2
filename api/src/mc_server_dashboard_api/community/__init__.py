"""Community context: isolation/ownership units, membership, roles, grants.

Holds the community bounded context's quadrants (domain / adapters). This
sub-issue lands the pure domain and its persistence; the permission checker,
use cases, and HTTP edge land with their own features (epic #5).
"""
