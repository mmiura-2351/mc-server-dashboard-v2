"""Application layer for the ``servers`` context: CRUD use cases.

Use cases orchestrate the domain through its Ports; they assume the route's
two-layer authorization dependency has already admitted the caller (Section 6.4).
"""
