# Local tool installs (e.g. pinned golangci-lint via `make` / install step).
/.bin/

# Test and coverage artifacts.
*.test
*.out
coverage.txt

# Compiled binaries from `go build` in the module root.
/worker
