// Package domain holds the Worker's pure core: entities, value objects, domain
// errors, and Port interfaces. It depends on the standard library only and must
// not import any framework, driver, transport client, or other layer
// (see docs/app/ARCHITECTURE.md Section 2). It is empty until domain logic lands.
package domain
