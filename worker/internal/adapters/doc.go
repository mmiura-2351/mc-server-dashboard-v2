// Package adapters holds concrete implementations of domain Ports (for example
// the host-process and container execution drivers). Adapters may use any
// framework or external library, but must never be imported by the domain or
// application layers (see docs/app/ARCHITECTURE.md Section 2). It is empty until
// adapters land.
package adapters
